language: rust

rust:
  - stable
  - nightly

os:
  - windows
  - linux

script:
- cargo test

matrix:
  include:
  - name: clippy
    rust: stable
    install:
    - rustup component add clippy-preview
    script:
    - cargo clippy --lib --tests
